return {
  -- 'karb94/neoscroll.nvim',
  -- config = function()
  --   require('neoscroll').setup({
  --     -- All keys will be mapped to their corresponding default scrolling animation
  --     mappings = {'<C-u>', '<C-d>', '<C-b>', '<C-f>', 'zt', 'zz', 'zb'},
  --     hide_cursor = true,          -- Hide cursor while scrolling
  --     stop_eof = true,             -- Stop at end of file
  --     respect_scrolloff = false,   -- Stop scrolling when the cursor reaches the scrolloff margin
  --     cursor_scrolls_alone = true, -- The cursor will keep on scrolling even if the window cannot scroll further
  --     easing_function = "quadratic", -- The easing function to use
  --     pre_hook = nil,
  --     post_hook = nil,
  --     performance_options = {
  --       -- High frame rate for a smoother experience
  --       framerate = 60,
  --     }
  --   })
  -- end
}
